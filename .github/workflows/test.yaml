name: Catalog Contract Test

on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_call:

jobs:
  launch-parity-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Run keyed TOML launch parity matrix
        run: ./scripts/run-launch-parity-matrix.sh

      - name: Upload keyed TOML parity evidence artifacts
        uses: actions/upload-artifact@v4
        with:
          name: launch-parity-contract-evidence
          path: .sisyphus/evidence/
